# Learn modules – MCP test

**See `all-learn-modules-mcp.spec.md`** for the full "all learn modules" test procedure (login → each topic → restart → click through content).

**Quick reference:** Use Chrome DevTools or cursor-ide-browser MCP:
- Navigate → Snapshot → Fill/Click → repeat until done.
- Credentials from `.env`: `TEST_USERNAME`, `TEST_PASSWORD`.
